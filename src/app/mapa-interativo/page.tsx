import { MapaInterativoLoader } from "@/components/page/MapaInterativoLoader";

export default function MapaInterativoPage() {
  return (
    <div className="flex-1 flex flex-col bg-zinc-100">
      <MapaInterativoLoader />
    </div>
  );
}